# MongoDB connection URI (Atlas string or local MongoDB URI)
MONGO_URI=mongodb://localhost:27017/dailynews

# Port for backend Express server to listen on
PORT=5001

# Node environment mode (development or production)
NODE_ENV=development

# Passcode to access the admin dashboard control panel
ADMIN_PASSCODE=admin123
